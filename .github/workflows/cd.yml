# CD: Build & push Docker images to GitHub Container Registry (on release)
# Public repo: uses GITHUB_TOKEN (auto), no extra secret needed

name: CD

on:
  release:
    types: [published]
  # Optional: also build & push image latest on push to main
  # push:
  #   branches: [main]

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    name: Build & push images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set version from release tag
        id: meta
        run: echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Backend
        uses: docker/build-push-action@v6
        with:
          context: ./lake-flow
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/datalake-backend:${{ steps.meta.outputs.version }}
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/datalake-backend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push Frontend
        uses: docker/build-push-action@v6
        with:
          context: ./lake-flow-ui
          file: ./lake-flow-ui/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/datalake-frontend:${{ steps.meta.outputs.version }}
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/datalake-frontend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
